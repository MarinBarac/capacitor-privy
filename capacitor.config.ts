import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor_privy.app',
  appName: 'capacitor-privy',
  webDir: 'dist'
};

export default config;
